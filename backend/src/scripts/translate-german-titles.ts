/**
 * DEPRECATED — This script requires OPENAI_API_KEY which is not configured.
 *
 * Use fix-german-titles-covers.ts instead — it does the same job (and more)
 * using the already-configured GEMINI_API_KEY:
 *
 *   npx tsx src/scripts/fix-german-titles-covers.ts
 */
throw new Error('This script is deprecated. Run fix-german-titles-covers.ts instead.');
